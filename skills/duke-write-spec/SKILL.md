---
name: duke-write-spec
description: 将已经确认的结构化需求、业务规则和验收标准整理为轻量 Spec、标准需求文档、完整 PRD 或一页看板，并按既定文档规范保存。适用于需求内容已经基本明确、主要需要规范化撰写和沉淀的场景。不负责从模糊需求开展完整访谈、独立设计验收场景、评审成品文档或核对实际实现。
---

# 产品需求文档输出

## 工作目标

将已确认的需求包和验收标准组装成深度适当、研发可执行的产品文档，不重新发明业务规则。

## 输入要求

优先接收：

- 需求主题、背景、当前问题和目标；
- 本期范围和明确排除项；
- 带 `R-xxx` 编号的业务规则；
- 流程、状态、权限、异常和系统影响中的适用内容；
- 带 `AC-xxx` 编号的验收标准；
- 交付等级 L1、L2 或 L3。

缺少会改变核心方案的信息时返回阻塞项，不开展完整访谈。只有非阻塞信息缺失时可以输出标记清晰的草稿。

## 工作流程

正式输出前读取 [文档输出契约](references/spec-contracts.md)。

1. 采用传入的交付等级；直接调用且未提供等级时，按影响范围和风险选择最轻的可用等级。
2. 将确认事实写成确定结论，将建议和待确认项单独标识。
3. 按契约组织章节，省略不适用内容和空表。
4. 保留业务规则和验收标准的编号及对应关系。
5. 检查规则、流程、状态和文案是否在全文一致。
6. 只有用户明确要求时写入本地文件。

## 表达要求

- 默认使用中文，简洁、结论化、可执行、可验证。
- 多个字段、状态、规则、角色或场景需要比较时使用表格；简单内容不强制制表。
- 使用精确状态名、字段名、按钮名、提示语和跟进记录文案。
- 不为形式虚构指标、数据、用户证据、异常或系统影响。
- 不适用章节直接省略；只有省略可能导致误解时才写“不涉及”。

## 版本和文档沉淀

只有用户要求版本化正式文档时维护版本号和更新记录。初始正式版本使用 `V1.0`；小范围补充递增 `V1.1`、`V1.2`；重大结构或流程变化升级为 `V2.0`。

用户要求创建或更新文件时：

- 文件名使用“业务主题-文档类型.md”，避免“最终版”“最新版”；
- 若仓库存在 `docs/README.md` 和主题目录，先读取其规范和相关模板；
- 按分类保存文档并更新索引；
- 将不再维护的旧版本移入既有归档目录，不自行建立新的治理规则。

## 输出边界

- 不从模糊想法重新开展完整需求访谈。
- 不独立生成详细验收场景；验收标准缺失时保留待补充提示或建议使用 `duke-write-acceptance`。
- 不对成品进行全面产品评审；需要时交给 `duke-review-spec`。
- 不读取代码或截图判断实现是否符合需求。
