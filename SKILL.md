---
name: duke-doc
description: 通过多轮中文对话梳理业务需求，并输出可交付研发的规范产品需求文档（PRD/Spec）、一页看板或验收标准。适用于需求访谈、流程和状态设计、字段与权限规则、系统联动、异常处理及产品文档沉淀。
---

# 产品需求访谈与文档输出

## 访谈流程

1. 先简要复述已知的背景、目标、约束和结论。
2. 每轮只追问一个最影响方案的未决问题。依次确认触发条件、参与对象和前置条件、状态限制、交互默认值、人工覆盖、异常处理、记录和验收口径。
3. 将用户已确认的规则用确定性表述沉淀；不要把假设写成结论。
4. 将未定事项记录为待确认项、风险、依赖或边界问题。
5. 信息齐全后再输出正式文档；用户仅需要快速对齐时，输出一页看板。

## 正式 PRD 默认结构

使用以下章节和顺序：

1. 更新记录
2. 需求背景及目标
3. 名词解释
4. 功能结构
5. 业务流程
6. 需求详情
7. 其他问题

在“更新记录”中包含文档信息表：文档名称、文档类型、当前版本、创建日期、最近更新、维护方式；并保留版本记录表。初始正式版本使用 `V1.0`；小范围补充递增 `V1.1`、`V1.2`；重大结构或流程变化升级为 `V2.0`。

## 表达要求

- 明确操作角色、权限、流程、字段、状态、默认值、校验、异常、边界和验收口径。
- 将字段、状态、规则、权限和验收场景优先写成表格；复杂规则可在表后补充说明。
- 使用可执行、可验证的语句，写出精确状态名、按钮名、提示语和跟进记录文案。
- 对系统联动或失败场景，说明主操作是否成功、关联操作是否执行、是否回滚、是否记录及用户提示。
- 验收标准使用“场景 / 操作 / 预期结果”表格。

## 交付深度

- 简单需求：输出需求摘要、Spec 和验收标准。
- 中大型需求：额外输出 Demo/原型说明、研发影响说明、字段规则表、状态流转表、待确认清单；涉及迁移、权限切换、计费或结算时补充专项方案。
- 快速会议对齐：输出“一页看板”，保留阶段目标、主导角色、关键动作、完成标志和高风险提醒，并关联详细文档。

## 文档沉淀

仅在用户要求创建或更新文件时写入工作区。文件名使用“业务主题-文档类型.md”，避免“最终版”“最新版”等名称。若仓库存在 `docs/README.md` 和主题目录，按其分类存放文档、更新索引，并将不再维护的版本移入“归档”。
