---
name: duke-doc
description: 通过多轮中文对话判断需求价值、用户问题、范围、MVP 和成功指标，再梳理流程、规则、边界场景、系统影响与非功能需求并输出可交付研发的规范产品需求文档（PRD/Spec）、一页看板或验收标准。适用于需求访谈、需求评审、方案收敛、状态与权限设计、系统联动、异常处理及产品文档沉淀。
---

# 产品需求分析、访谈与文档输出

## 工作原则

- 不只记录用户提出的解决方案；先判断要解决的问题、目标用户和预期价值。
- 优先询问会影响需求价值、范围或方案选择的问题，避免重复确认已知信息。
- 将用户确认的事实和规则写成确定结论；将推测、建议和未确认事项明确区分。
- 默认使用中文，表述简洁、结论化、可执行、可验证。

## 工作流程

### 1. 复述需求

先简要复述已知的背景、问题、目标、约束和结论，指出当前最关键的信息缺口。

### 2. 判断需求价值

在进入功能细节前，确认或推导以下内容：

- 目标用户或受影响角色是谁；
- 当前在什么场景下遇到什么问题，现有流程如何处理；
- 问题发生频率、影响程度或业务损失；
- 期望改善的用户结果或业务结果；
- 为什么现在需要解决，不处理会产生什么影响；
- 是否存在数据、反馈、案例或其他证据支撑。

若价值依据不足，指出风险并继续澄清；不要把用户提出的功能方案直接等同于真实需求。

### 3. 管理范围与优先级

明确以下边界：

- 本期范围：本次必须解决的用户、场景、流程和能力；
- 非本期范围：本次明确不处理的内容；
- MVP：能形成最小业务闭环的必要能力；
- 后续优化：不阻塞 MVP、可在后续迭代补充的能力；
- 优先级：区分必须实现、建议实现和后续考虑，并说明判断依据。

发现目标、资源、时限或范围冲突时，明确指出取舍，不默认全部纳入。

### 4. 定义成功指标

优先从效率、转化、质量、成本、风险或用户体验中选择与目标直接相关的指标。尽量明确当前基线、目标值、观察周期和数据来源。

无法量化时，定义可验证的定性完成标准，并标记后续需要补充的数据口径；不要为了形式虚构指标。

### 5. 梳理方案与规则

依次确认触发条件、参与对象、前置条件、主流程、替代流程、失败流程、状态限制、交互默认值、人工覆盖、权限、异常处理、操作记录和验收口径。

交互说明至少覆盖入口、展示条件、可用条件、默认值、禁用原因、确认提示、处理反馈、失败提示、撤销或人工补救能力，以及不同角色看到的差异。

默认每轮询问一个最影响决策的问题。以下情况可一次询问 2～3 个高度相关的问题：

- 必须组合回答才能判断同一项规则；
- 用户明确要求快速或批量推进；
- 问题简单且不会造成较高理解负担。

复杂选择尽量提供简短选项或示例，帮助用户快速确认。非阻塞信息可先标记待确认，不因小问题无限延迟文档输出。

### 6. 评审边界、系统影响与非功能需求

中大型需求，或涉及系统联动、状态流转、数据、接口、权限、存量兼容时，先读取 [产品评审检查表](references/product-review-checklists.md)，再完成正式输出。

至少判断以下维度是否适用：

- 边界与一致性：重复提交、并发操作、幂等、超时、重试、部分成功、状态冲突和数据一致性；
- 系统影响：涉及模块、上下游系统、接口、数据结构、权限、日志、审计、监控、存量数据和依赖方；
- 非功能需求：性能、可用性、安全、隐私、兼容性、可恢复性和可观测性；
- 上线影响：灰度范围、存量兼容、迁移方案、回滚条件和上线后观察点。

只输出适用内容。对明显不适用的维度可标记“不适用”并说明原因，不机械生成空表。

### 7. 管理待确认项与需求变更

待确认项至少记录问题、当前建议、责任人、截止时间、是否阻塞和未确认时的处理方式。阻塞项未解决时不得输出无标记的正式定稿；可以输出评审草案并显著提示阻塞风险。

修改已有需求时，记录变更原因、原规则、新规则、影响范围、是否影响排期或验收，以及是否需要产品、研发、测试或业务重新评审。同步更新版本号和更新记录。

### 8. 执行需求就绪检查

正式输出前检查以下内容：

- 背景、目标用户、当前问题和现有流程已明确；
- 需求价值和预期结果已明确；
- 本期范围、非本期范围、MVP 和优先级已明确；
- 成功指标或定性完成标准已明确；
- 角色、前置条件、主流程、状态和核心规则已明确；
- 关键交互状态、异常、边界条件和人工兜底方式已明确；
- 适用的系统影响和非功能需求已评审；
- 核心规则已映射到主流程、异常流程或验收场景；
- 验收口径覆盖正常、异常、边界和权限差异；
- 待确认项、风险和依赖已记录责任人与阻塞状态。

缺少会改变价值、范围或核心方案的信息时，继续访谈。仅缺少非阻塞细节时，可以输出草案，但必须显著标记待确认内容。

## 正式 PRD 默认结构

使用以下章节和顺序：

1. 更新记录
2. 需求背景及目标
3. 名词解释
4. 功能结构
5. 业务流程
6. 需求详情
7. 其他问题

在“需求背景及目标”中包含目标用户、当前问题、需求价值、范围、非范围、MVP 和成功指标。在“需求详情”中按需包含交互规则、状态与字段、边界场景、系统影响、非功能需求和验收口径。在“更新记录”中包含文档名称、文档类型、当前版本、创建日期、最近更新和维护方式，并保留版本记录表。初始正式版本使用 `V1.0`；小范围补充递增 `V1.1`、`V1.2`；重大结构或流程变化升级为 `V2.0`。

## 表达要求

- 明确操作角色、权限、流程、字段、状态、默认值、校验、异常、边界和验收口径。
- 将字段、状态、规则、权限和验收场景优先写成表格；复杂规则可在表后补充说明。
- 使用可执行、可验证的语句，写出精确状态名、按钮名、提示语和跟进记录文案。
- 对系统联动或失败场景，说明主操作是否成功、关联操作是否执行、是否回滚、是否记录及用户提示。
- 禁用或不可操作状态需说明原因、用户是否可见及后续处理方式。
- 验收标准使用“场景 / 操作 / 预期结果”表格，并覆盖正常、异常、边界和权限差异。
- 关键业务规则应能追溯到对应流程、页面交互或验收场景。

## 交付深度

- 简单需求：输出需求摘要、Spec 和验收标准。
- 中大型需求：额外输出 Demo/原型说明、研发影响说明、字段规则表、状态流转表、边界与异常清单、非功能需求和待确认清单；涉及迁移、权限切换、计费或结算时补充专项方案。
- 快速会议对齐：输出“一页看板”，保留需求价值、目标指标、范围、阶段目标、主导角色、关键动作、完成标志和高风险提醒，并关联详细文档。

## 文档沉淀

仅在用户要求创建或更新文件时写入工作区。文件名使用“业务主题-文档类型.md”，避免“最终版”“最新版”等名称。若仓库存在 `docs/README.md` 和主题目录，先读取其规范和相关模板，再按分类存放文档、更新索引，并将不再维护的版本移入“归档”。
